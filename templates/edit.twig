<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="x-ua-compatible">
        <title>The Hobbit: A Sample</title>
        <link href='https://fonts.googleapis.com/css?family=Noto+Serif:400,400italic,700|Noto+Sans:700,400' rel='stylesheet' type='text/css'>
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
        <link rel="stylesheet" href="/../css/normalize.css">
        <link rel="stylesheet" href="/../css/site.css">
    </head>
    <body>
        <header>
            <div class="container">
                <div class="site-header">
                    <a style="color:green" class="logo" href="/">The Hobbit: A Sample</a>
                    <a class="new-entry button button-round" href="/new"><i class="material-icons">create</i></a>
                </div>
            </div>
        </header>
        <section>
            <div class="container">
                <div class="edit-entry">
                    <h2>Edit Entry</h2>
                    <form action="/edit/{{ post.id }}" method="POST">
							<label for="title"> Title <span class="required-star">*</span></label>
							<input type="text" name="title"  value="{{ post.title }}"><br>
							<label for="date"></label>
							<label for="date"></label>
							<input type="hidden" name="date" value="{{ post.date }}"><br>
							<label for="entry">Entry <span class="required-star">*</span></label>
							<textarea rows="5" name="body">{{ post.body }}</textarea>
                            <input type="submit" value="Save" class="button">
					<a href="/detail/post/{{ post.id }}" class="button button-secondary">Cancel</a>
				</form>
				<br><br>
				<form action="/delete/{{ post.id}}" method="POST">
					<input type="submit" value="Delete" name="delete" class="button">
				</form>
                </div>
            </div>
        </section>
        <footer>
            <div>
                <a href="#">Contact Us</a>&nbsp;&nbsp;&bull;&nbsp;&nbsp;
                <a href="#">Terms</a>
            </div>
        </footer>
    </body>
</html>