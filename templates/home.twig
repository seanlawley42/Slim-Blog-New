<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="x-ua-compatible">
        <title>The Hobbit: A Sample</title>
        <link href='https://fonts.googleapis.com/css?family=Noto+Serif:400,400italic,700|Noto+Sans:700,400' rel='stylesheet' type='text/css'>
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
        <link rel="stylesheet" href="css/normalize.css">
        <link rel="stylesheet" href="css/site.css">
    </head>
    <body>
        <header>
            <div class="container">
                <div class="site-header">
                    <a style="color:green" class="logo" href="/">The Hobbit: A Sample</a>
                    <a class="new-entry button button-round" href="/new"><i class="material-icons">create</i></a>
                </div>
            </div>
        </header>
        <section>
            <div class="container">
                <div class="entry-list">
                {% for post in posts %}
                  <article>
                    <h2>
                        <a style="color:green" href="/detail/post/{{ post.id }}">{{ post.title }}</a>
                    </h2>
                    <h3>
                    <time datetime="2016-01-31 1:00">{{ item.date|date("m-d-Y") }}</time>
                    </h3>
                    <br>
                    <div>
                        <a style="color:white" a href="detail/post/{{ post.id }}" class="button">View post</a>
                    </div>
                    <div class="container">
                        <form method="post" action="/delete/{{ post.id }}" class="button">
                            <input type="submit" value="Delete" class="button">
                    </form>
                    </div>
                  </article>
                {% endfor %}
                </div>
            </div>
        </section>
        <footer>
            <div>
            <a href="#">Contact Us</a>&nbsp;&nbsp;&bull;&nbsp;&nbsp;
                <a href="#">Terms</a>
            </div>
        </footer>
    </body>
</html>